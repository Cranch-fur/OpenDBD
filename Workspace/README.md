# Workspace
This directory contains details about the build environment used at the time of the game's compilation. This data was extracted from the PDB (Program Database) file.

**Note:** The extraction process does not preserve case sensitivity (original capitalization). However, we have manually restored the correct casing for some of the paths.